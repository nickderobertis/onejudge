{
  "failure_report": {
    "$defs": {
      "CandidateAttempt": {
        "description": "One candidate identity oneharness attempted for a single invocation \u2014 the\nharness (and variant/model) it tried, and what came of it.\n\nUnder `run_mode = \"fallback\"` oneharness attempts candidates in priority order\nand stops at the first that runs the task, so an invocation can have several of\nthese: the ones it fell through, then the one that ran. This is the typed form\nof oneharness's per-candidate record \u2014 the thing that lets a consumer attribute\na failure to a *side* (agent vs judge) and an *identity* (which harness, which\naccount) instead of grepping a message.",
        "properties": {
          "available": {
            "description": "Whether oneharness found the harness binary.",
            "type": "boolean"
          },
          "duration_ms": {
            "description": "Wall-clock duration of this attempt.",
            "format": "uint64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "error": {
            "description": "oneharness's human-readable problem statement for a failed attempt.",
            "type": [
              "string",
              "null"
            ]
          },
          "exit_code": {
            "description": "The harness process's exit code, when it ran to completion.",
            "format": "int32",
            "type": [
              "integer",
              "null"
            ]
          },
          "failure_kind": {
            "description": "oneharness's normalized failure reason (`auth`, `rate_limit`,\n`model_not_found`, `quota`, `tool_deferred`, `session_not_found`,\n`untrusted_directory`, `input_too_large`, `model_mismatch`), when it\nclassified one.",
            "type": [
              "string",
              "null"
            ]
          },
          "failure_kind_source": {
            "description": "Where that reason was read (`stderr`, `stdout`, `config:env_from`).",
            "type": [
              "string",
              "null"
            ]
          },
          "harness": {
            "description": "Canonical harness id (e.g. `claude-code`).",
            "type": "string"
          },
          "harness_id": {
            "description": "Base id, or `<base>:<variant>` when a named preset selected the identity \u2014\nthe selector that reproduces this candidate.",
            "type": "string"
          },
          "history_id": {
            "description": "The oneharness history record written for this attempt, when history was\non \u2014 the handle `oneharness history show` resolves.",
            "type": [
              "string",
              "null"
            ]
          },
          "model": {
            "description": "The model this candidate ran with, when one was requested.",
            "type": [
              "string",
              "null"
            ]
          },
          "ran": {
            "description": "Whether this is the candidate that actually ran the turn.",
            "type": "boolean"
          },
          "session_id": {
            "description": "The harness's own continuation id, when it exposed one.",
            "type": [
              "string",
              "null"
            ]
          },
          "status": {
            "description": "oneharness's status token: `ok`, `nonzero`, `timeout`, `spawn-error`,\n`skipped`, or `planned`.",
            "type": "string"
          },
          "usage": {
            "anyOf": [
              {
                "$ref": "#/$defs/Usage"
              },
              {
                "type": "null"
              }
            ],
            "description": "This attempt's own token/cost accounting."
          },
          "variant": {
            "description": "The named preset, when this candidate came from a composed harness id.",
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "harness",
          "harness_id",
          "status",
          "available",
          "ran"
        ],
        "type": "object"
      },
      "Decision": {
        "description": "What one judge of a [`JudgePanel`](crate::JudgePanel) decided, as a wire token.\n\nThe five outcomes a supervisor call can have, plus `error` for a judge that\ncould not run \u2014 recorded rather than dropped, because a judge that could not\nrun is never read as a pass and a reader has to be able to see that it was\nasked.",
        "oneOf": [
          {
            "const": "done",
            "description": "The judge declared the work complete.",
            "type": "string"
          },
          {
            "const": "continue",
            "description": "The judge handed back a next instruction.",
            "type": "string"
          },
          {
            "const": "no_instruction",
            "description": "The judge judged the work incomplete but named no next instruction, even\nasked again.",
            "type": "string"
          },
          {
            "const": "unparseable",
            "description": "The judge answered in neither documented shape, even asked again.",
            "type": "string"
          },
          {
            "const": "error",
            "description": "The judge's call failed; `reason` is the error's message.",
            "type": "string"
          }
        ]
      },
      "FailureDetail": {
        "description": "The failure itself, with the classification a caller branches on.",
        "properties": {
          "kind": {
            "anyOf": [
              {
                "$ref": "#/$defs/ProviderErrorKind"
              },
              {
                "type": "null"
              }
            ],
            "description": "The provider failure category, when the failure came from a provider and\nwas classified (`auth`, `quota`, `timeout`, `spawn`, `protocol`, \u2026)."
          },
          "message": {
            "description": "The human-readable failure, exactly as it is printed on stderr.",
            "type": "string"
          }
        },
        "required": [
          "message"
        ],
        "type": "object"
      },
      "FellThrough": {
        "description": "One candidate a fallback chain fell through, with oneharness's reason token\n(`not-installed`, `spawn-error`, `auth`, `quota`, `model-not-found`,\n`rate-limit`, `session-not-found`, `untrusted-directory`, `input-too-large`,\n`model-mismatch`).",
        "properties": {
          "harness": {
            "description": "Canonical harness id.",
            "type": "string"
          },
          "reason": {
            "description": "Why it could not run the task at all.",
            "type": "string"
          }
        },
        "required": [
          "harness",
          "reason"
        ],
        "type": "object"
      },
      "HarnessAttribution": {
        "description": "What oneharness attempted for one provider invocation, placed on the side and\nturn that made the call.",
        "properties": {
          "candidates": {
            "description": "Every attempted candidate, in the order oneharness tried them.",
            "items": {
              "$ref": "#/$defs/CandidateAttempt"
            },
            "type": "array"
          },
          "fell_through": {
            "description": "Candidates a fallback chain routed around, in priority order.",
            "items": {
              "$ref": "#/$defs/FellThrough"
            },
            "type": "array"
          },
          "history_file": {
            "description": "The oneharness history session file this invocation appended to.",
            "type": [
              "string",
              "null"
            ]
          },
          "judge": {
            "description": "Which judge of a [`JudgePanel`](crate::JudgePanel) made the invocation, by\nlabel \u2014 as [`SessionLink::judge`], and absent under the same conditions.",
            "type": [
              "string",
              "null"
            ]
          },
          "ran": {
            "description": "The candidate that ran the turn, by composed id; `null` when none could.",
            "type": [
              "string",
              "null"
            ]
          },
          "role": {
            "$ref": "#/$defs/TelemetryRole",
            "description": "Which side of the conversation made this call."
          },
          "turn_index": {
            "description": "One-based invocation index within the role.",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          }
        },
        "required": [
          "role",
          "turn_index",
          "candidates"
        ],
        "type": "object"
      },
      "JudgeDecision": {
        "description": "One judge's decision on one judge-side call, attributed to the judge by its\npanel label and provider kind.\n\nA [`JudgePanel`](crate::JudgePanel) records one per judge per supervisor call\n\u2014 including the call that failed \u2014 and the engine drains them after every\nsupervisor call, `Ok` or `Err`, onto [`Report::judge_decisions`]. A provider\nthat is not a panel records none, so a report carrying no decisions was judged\nby a bare provider; nothing is synthesized.",
        "properties": {
          "decision": {
            "$ref": "#/$defs/Decision",
            "description": "What it decided, or that it failed."
          },
          "judge": {
            "description": "The judge's label within its panel (`label:` in the config, or the\ndefaulted `<kind>` / `<kind>-<n>`).",
            "type": "string"
          },
          "kind": {
            "description": "The judge entry's provider kind (`oneharness`, `command`, \u2026).",
            "type": "string"
          },
          "reason": {
            "description": "Its own reason \u2014 or, for an `error`, the error's message.",
            "type": "string"
          }
        },
        "required": [
          "judge",
          "kind",
          "decision",
          "reason"
        ],
        "type": "object"
      },
      "JudgedTurn": {
        "description": "Every judge's decision on one supervisor turn.",
        "properties": {
          "decisions": {
            "description": "One decision per judge, in the panel's list order.",
            "items": {
              "$ref": "#/$defs/JudgeDecision"
            },
            "type": "array"
          },
          "turn": {
            "description": "1-based assistant-turn index the supervisor turn belongs to, as\n[`TurnOpened::turn`](crate::TurnOpened::turn).",
            "format": "uint",
            "minimum": 0,
            "type": "integer"
          }
        },
        "required": [
          "turn",
          "decisions"
        ],
        "type": "object"
      },
      "PartyTelemetry": {
        "description": "Aggregated measurements for one side of a run.",
        "properties": {
          "model_ms": {
            "description": "Model/provider elapsed time, when every contributing invocation reported it.",
            "format": "uint64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "session_ids": {
            "description": "Native oneharness session IDs in first-seen order, without duplicates.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "time_to_first_token_ms": {
            "description": "Time to first token from the first invocation that reported it.",
            "format": "uint64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "tool_ms": {
            "description": "Tool elapsed time, when every contributing invocation reported it.",
            "format": "uint64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "usage": {
            "anyOf": [
              {
                "$ref": "#/$defs/Usage"
              },
              {
                "type": "null"
              }
            ],
            "description": "Strictly aggregated usage; each field is present only when every invocation knew it."
          }
        },
        "type": "object"
      },
      "ProviderErrorKind": {
        "description": "Machine-readable classification of a provider failure.\n\nThe category a consumer branches on instead of matching substrings in the\nhuman message (the reason this exists: a timeout is\n[`ProviderErrorKind::Timeout`], not `message.contains(\"timed out\")`). It maps\ndirectly onto `oneharness`'s normalized `failure_kind`. The set is closed on\npurpose; a label this version does not recognize becomes\n[`ProviderErrorKind::Other`] rather than a new, unhandled string.",
        "oneOf": [
          {
            "const": "auth",
            "description": "Authentication/authorization failed (missing or rejected credentials).",
            "type": "string"
          },
          {
            "const": "rate_limit",
            "description": "The provider rate-limited the call; a backoff-and-retry may succeed.",
            "type": "string"
          },
          {
            "const": "model_not_found",
            "description": "The harness/vendor does not recognize the requested model.",
            "type": "string"
          },
          {
            "const": "quota",
            "description": "The account's quota or billing limit is exhausted.",
            "type": "string"
          },
          {
            "const": "overloaded",
            "description": "A transient server-side overload.",
            "type": "string"
          },
          {
            "const": "timeout",
            "description": "The call exceeded its deadline.",
            "type": "string"
          },
          {
            "const": "cancelled",
            "description": "The run was torn down before it finished because it was **cancelled** \u2014\nthe caller stopped the turn, or the host was signalled. Distinct from\n[`ProviderErrorKind::Timeout`], which was given its full deadline and\nexceeded it: a cancelled call says nothing about whether the harness was\nhealthy, so it is not a reason to give up on the environment.",
            "type": "string"
          },
          {
            "const": "spawn",
            "description": "The provider process could not be started (binary missing, not runnable).",
            "type": "string"
          },
          {
            "const": "protocol",
            "description": "The provider ran but produced output that violated the protocol\n(unparseable envelope, missing results, malformed verdict).",
            "type": "string"
          },
          {
            "const": "other",
            "description": "A classified failure that does not map to a more specific category.",
            "type": "string"
          }
        ]
      },
      "SessionLink": {
        "description": "Authoritative linkage from a onejudge role/turn to a native oneharness run.",
        "properties": {
          "finished_at": {
            "description": "Upstream UTC invocation-finish timestamp, or `null` when not observed.",
            "type": [
              "string",
              "null"
            ]
          },
          "history_id": {
            "description": "Native oneharness history record identity, when history was available.",
            "type": [
              "string",
              "null"
            ]
          },
          "judge": {
            "description": "Which judge of a [`JudgePanel`](crate::JudgePanel) made the invocation, by\nlabel. Set only by a panel holding more than one judge; absent for a bare\nprovider and for a panel of one, whose records are the ones it has always\nwritten.",
            "type": [
              "string",
              "null"
            ]
          },
          "role": {
            "$ref": "#/$defs/TelemetryRole",
            "description": "Which side of the conversation owns the invocation."
          },
          "session_id": {
            "description": "Native harness continuation identifier.",
            "type": "string"
          },
          "started_at": {
            "description": "Upstream UTC invocation-start timestamp.",
            "type": "string"
          },
          "turn_index": {
            "description": "One-based invocation index within the role.",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          }
        },
        "required": [
          "session_id",
          "role",
          "turn_index",
          "started_at",
          "finished_at"
        ],
        "type": "object"
      },
      "SpawnedProcess": {
        "description": "One process onejudge spawned for a run, and the group an embedder's\n[`SpawnHook`] placed it in.\n\nCarried on [`Report::processes`](crate::Report::processes) (and on the CLI's\n`--format json` failure document), so what an in-process embedder can observe\nthrough the hook is also machine-readable from the command line.\n\n`group` is present **only** when a hook named one: onejudge never reports a\ngroup that does not exist, so a `null` group is the honest statement that this\nprocess is in whatever group onejudge itself was in.",
        "properties": {
          "group": {
            "description": "The embedder-owned group a [`SpawnHook`] reported placing it in.",
            "type": [
              "string",
              "null"
            ]
          },
          "judge": {
            "description": "Which judge of a [`JudgePanel`](crate::JudgePanel) spawned it, by label.\nSet only by a panel holding more than one judge; absent otherwise, so a\nsingle-judge run's records are the ones it has always written.",
            "type": [
              "string",
              "null"
            ]
          },
          "op": {
            "description": "The provider operation that asked for it.",
            "type": "string"
          },
          "pid": {
            "description": "The operating-system process id it was given.",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "program": {
            "description": "The program that was run (`argv[0]`).",
            "type": "string"
          },
          "role": {
            "$ref": "#/$defs/TelemetryRole",
            "description": "Which side of the conversation the process served."
          }
        },
        "required": [
          "role",
          "op",
          "program",
          "pid"
        ],
        "type": "object"
      },
      "Telemetry": {
        "description": "Timing, usage, and native linkage for the complete agent+judge run.",
        "properties": {
          "agent": {
            "$ref": "#/$defs/PartyTelemetry",
            "description": "Agent-side measurements."
          },
          "attribution": {
            "description": "Which harness identities each invocation attempted, in invocation order.\nEmpty when the provider reports no candidate identities.",
            "items": {
              "$ref": "#/$defs/HarnessAttribution"
            },
            "type": "array"
          },
          "judge": {
            "$ref": "#/$defs/PartyTelemetry",
            "description": "Simulated-user, supervisor, and evaluator measurements."
          },
          "orchestration_ms": {
            "description": "Non-negative wall-time remainder after measured model and tool work.",
            "format": "uint64",
            "minimum": 0,
            "type": "integer"
          },
          "sessions": {
            "default": [],
            "description": "Native session linkage records in invocation order.",
            "items": {
              "$ref": "#/$defs/SessionLink"
            },
            "type": "array"
          },
          "wall_ms": {
            "description": "Monotonic duration from task-loop entry through final result assembly.",
            "format": "uint64",
            "minimum": 0,
            "type": "integer"
          }
        },
        "required": [
          "wall_ms",
          "agent",
          "judge",
          "orchestration_ms",
          "sessions"
        ],
        "type": "object"
      },
      "TelemetryRole": {
        "description": "The side of the evaluated conversation that owns an invocation.",
        "oneOf": [
          {
            "const": "agent",
            "description": "The skill/agent under evaluation.",
            "type": "string"
          },
          {
            "const": "judge",
            "description": "The simulated user, supervisor, or final evaluator.",
            "type": "string"
          }
        ]
      },
      "Usage": {
        "description": "Token / cost usage for one provider call, or a running total.\n\nEach field is independently optional because not every harness reports every\nsignal (cost is commonly absent on subscription auth, and cache counts only\nwhen the harness surfaces prompt-cache reads/writes). `None` means \"no\nsignal\", never \"zero\" \u2014 so a total stays `None` until something reports a real\nnumber, then accumulates.",
        "properties": {
          "cache_read_tokens": {
            "description": "Prompt tokens served from the provider's prompt cache \u2014 a cheap read of a\npreviously-written prefix \u2014 when the harness reports them. `None` when the\nharness does not surface cache counts, never `0` as a guess.",
            "format": "uint64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "cache_write_tokens": {
            "description": "Prompt tokens written to the provider's prompt cache (a.k.a. cache\ncreation), when the harness reports them. `None` when not surfaced.",
            "format": "uint64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "cost_usd": {
            "description": "Total cost in USD, when reported.",
            "format": "double",
            "type": [
              "number",
              "null"
            ]
          },
          "input_tokens": {
            "description": "Prompt/input tokens billed, when reported.",
            "format": "uint64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "output_tokens": {
            "description": "Completion/output tokens billed, when reported.",
            "format": "uint64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          }
        },
        "type": "object"
      }
    },
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "description": "The machine-readable document a `--format json` run writes when it produced no\n[`Report`]: why it failed, and the telemetry \u2014 including per-invocation harness\nattribution \u2014 recorded before it did.\n\nStamped with the same [`SCHEMA_VERSION`](crate::SCHEMA_VERSION) as a report, so\none number describes both halves of the CLI's JSON surface. Additive: before\nthis existed a failed `--format json` run wrote nothing at all.",
    "properties": {
      "error": {
        "$ref": "#/$defs/FailureDetail",
        "description": "Why the run did not complete."
      },
      "judge_decisions": {
        "description": "What each judge of a panel decided on each supervisor turn before the\nfailure, the failed turn included. Absent for a bare provider.",
        "items": {
          "$ref": "#/$defs/JudgedTurn"
        },
        "type": "array"
      },
      "processes": {
        "description": "The processes the failed run had already spawned, with the embedder-owned\ngroup each was placed in. Absent when nothing was spawned.",
        "items": {
          "$ref": "#/$defs/SpawnedProcess"
        },
        "type": "array"
      },
      "schema_version": {
        "description": "The contract version this document was serialized under.",
        "format": "uint32",
        "minimum": 0,
        "type": "integer"
      },
      "telemetry": {
        "anyOf": [
          {
            "$ref": "#/$defs/Telemetry"
          },
          {
            "type": "null"
          }
        ],
        "description": "Timing, usage, and per-invocation harness attribution recorded before the\nfailure; absent when nothing ran."
      }
    },
    "required": [
      "schema_version",
      "error"
    ],
    "title": "FailureReport",
    "type": "object"
  },
  "frames": {
    "agent.onejudge-frame.assess@7": {
      "$defs": {
        "Message": {
          "description": "A single turn in the conversation.",
          "properties": {
            "content": {
              "description": "The turn's text.",
              "type": "string"
            },
            "events": {
              "description": "The normalized tool events the skill took producing this turn (assistant\nturns only, and only when the harness exposed a tool transcript). Empty\notherwise. Surfaced for post-hoc analysis, streamed live, and rendered\ninto the transcript the judge sees.",
              "items": {
                "$ref": "#/$defs/ToolEvent"
              },
              "type": "array"
            },
            "role": {
              "$ref": "#/$defs/Role",
              "description": "Who produced the turn."
            }
          },
          "required": [
            "role",
            "content"
          ],
          "type": "object"
        },
        "Role": {
          "description": "Who produced a message.",
          "oneOf": [
            {
              "const": "user",
              "description": "The (real or simulated) user driving the skill.",
              "type": "string"
            },
            {
              "const": "assistant",
              "description": "The skill / assistant under test.",
              "type": "string"
            },
            {
              "const": "system",
              "description": "System-level framing, if a provider surfaces it.",
              "type": "string"
            }
          ]
        },
        "ToolEvent": {
          "description": "One normalized tool-call / action event the skill took during a turn, lifted\nfrom `oneharness`'s `events` array (its `--events` output). Harness-agnostic,\nso a consumer can inspect shell commands, file edits, and tool uses across any\nharness, not just the final text. `input` is the structured, tool-shaped args\nso a consumer can match on the command string or file path without re-parsing.\n\n`input` is free-form JSON, so `Message`/`Transcript` are `PartialEq` but not\n`Eq`.",
          "properties": {
            "index": {
              "default": 0,
              "description": "Position within the run, so ordering (\"did X before Y\") is expressible.",
              "format": "uint",
              "minimum": 0,
              "type": "integer"
            },
            "input": {
              "description": "Structured tool arguments (the command, the file path); `None` when none."
            },
            "kind": {
              "description": "`tool_call` (the skill invoked a tool) or `tool_result` (the observation).",
              "type": "string"
            },
            "name": {
              "description": "Normalized tool name where knowable (e.g. `bash`, `edit_file`); `None` for\na `tool_result` or when the harness did not name it.",
              "type": [
                "string",
                "null"
              ]
            },
            "output": {
              "description": "The result/observation text, when the transcript exposed it.",
              "type": [
                "string",
                "null"
              ]
            },
            "tool_call_id": {
              "description": "The harness's own stable identity for this call, joining a `tool_call` to\nits `tool_result`. `None` means the harness exposed no identity \u2014 never an\nempty string, which would read as an id that happens to be blank.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        }
      },
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "description": "`assess`: write a free-text judgement.",
      "properties": {
        "messages": {
          "items": {
            "$ref": "#/$defs/Message"
          },
          "type": "array"
        },
        "op": {
          "const": "assess",
          "description": "The operation this frame asks for.",
          "type": "string"
        },
        "prompt": {
          "type": "string"
        }
      },
      "required": [
        "op",
        "prompt",
        "messages"
      ],
      "title": "AssessFrame",
      "type": "object"
    },
    "agent.onejudge-frame.judge@7": {
      "$defs": {
        "EvidencePayload": {
          "additionalProperties": false,
          "properties": {
            "artifacts": {
              "description": "Caller-named artifacts, resolved against the worktree (protocol v7).\nOmitted when none are named, so a v6 command sees a byte-identical request.",
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "history_files": {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "worktree": {
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "history_files"
          ],
          "type": "object"
        },
        "Message": {
          "description": "A single turn in the conversation.",
          "properties": {
            "content": {
              "description": "The turn's text.",
              "type": "string"
            },
            "events": {
              "description": "The normalized tool events the skill took producing this turn (assistant\nturns only, and only when the harness exposed a tool transcript). Empty\notherwise. Surfaced for post-hoc analysis, streamed live, and rendered\ninto the transcript the judge sees.",
              "items": {
                "$ref": "#/$defs/ToolEvent"
              },
              "type": "array"
            },
            "role": {
              "$ref": "#/$defs/Role",
              "description": "Who produced the turn."
            }
          },
          "required": [
            "role",
            "content"
          ],
          "type": "object"
        },
        "Role": {
          "description": "Who produced a message.",
          "oneOf": [
            {
              "const": "user",
              "description": "The (real or simulated) user driving the skill.",
              "type": "string"
            },
            {
              "const": "assistant",
              "description": "The skill / assistant under test.",
              "type": "string"
            },
            {
              "const": "system",
              "description": "System-level framing, if a provider surfaces it.",
              "type": "string"
            }
          ]
        },
        "ToolEvent": {
          "description": "One normalized tool-call / action event the skill took during a turn, lifted\nfrom `oneharness`'s `events` array (its `--events` output). Harness-agnostic,\nso a consumer can inspect shell commands, file edits, and tool uses across any\nharness, not just the final text. `input` is the structured, tool-shaped args\nso a consumer can match on the command string or file path without re-parsing.\n\n`input` is free-form JSON, so `Message`/`Transcript` are `PartialEq` but not\n`Eq`.",
          "properties": {
            "index": {
              "default": 0,
              "description": "Position within the run, so ordering (\"did X before Y\") is expressible.",
              "format": "uint",
              "minimum": 0,
              "type": "integer"
            },
            "input": {
              "description": "Structured tool arguments (the command, the file path); `None` when none."
            },
            "kind": {
              "description": "`tool_call` (the skill invoked a tool) or `tool_result` (the observation).",
              "type": "string"
            },
            "name": {
              "description": "Normalized tool name where knowable (e.g. `bash`, `edit_file`); `None` for\na `tool_result` or when the harness did not name it.",
              "type": [
                "string",
                "null"
              ]
            },
            "output": {
              "description": "The result/observation text, when the transcript exposed it.",
              "type": [
                "string",
                "null"
              ]
            },
            "tool_call_id": {
              "description": "The harness's own stable identity for this call, joining a `tool_call` to\nits `tool_result`. `None` means the harness exposed no identity \u2014 never an\nempty string, which would read as an id that happens to be blank.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        }
      },
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "description": "`judge`: score a criterion against the transcript. A numeric score carries its\nbounds; a boolean one has none.",
      "oneOf": [
        {
          "additionalProperties": false,
          "properties": {
            "criterion": {
              "type": "string"
            },
            "evidence": {
              "anyOf": [
                {
                  "$ref": "#/$defs/EvidencePayload"
                },
                {
                  "type": "null"
                }
              ]
            },
            "kind": {
              "const": "boolean",
              "type": "string"
            },
            "messages": {
              "items": {
                "$ref": "#/$defs/Message"
              },
              "type": "array"
            },
            "op": {
              "const": "judge",
              "description": "The operation this frame asks for.",
              "type": "string"
            }
          },
          "required": [
            "op",
            "kind",
            "criterion",
            "messages"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "criterion": {
              "type": "string"
            },
            "evidence": {
              "anyOf": [
                {
                  "$ref": "#/$defs/EvidencePayload"
                },
                {
                  "type": "null"
                }
              ]
            },
            "kind": {
              "const": "numeric",
              "type": "string"
            },
            "max": {
              "format": "double",
              "type": "number"
            },
            "messages": {
              "items": {
                "$ref": "#/$defs/Message"
              },
              "type": "array"
            },
            "min": {
              "format": "double",
              "type": "number"
            },
            "op": {
              "const": "judge",
              "description": "The operation this frame asks for.",
              "type": "string"
            }
          },
          "required": [
            "op",
            "kind",
            "criterion",
            "min",
            "max",
            "messages"
          ],
          "type": "object"
        }
      ],
      "properties": {
        "op": {
          "const": "judge",
          "description": "The operation this frame asks for.",
          "type": "string"
        }
      },
      "required": [
        "op"
      ],
      "title": "JudgeFrame"
    },
    "agent.onejudge-frame.respond@7": {
      "$defs": {
        "Message": {
          "description": "A single turn in the conversation.",
          "properties": {
            "content": {
              "description": "The turn's text.",
              "type": "string"
            },
            "events": {
              "description": "The normalized tool events the skill took producing this turn (assistant\nturns only, and only when the harness exposed a tool transcript). Empty\notherwise. Surfaced for post-hoc analysis, streamed live, and rendered\ninto the transcript the judge sees.",
              "items": {
                "$ref": "#/$defs/ToolEvent"
              },
              "type": "array"
            },
            "role": {
              "$ref": "#/$defs/Role",
              "description": "Who produced the turn."
            }
          },
          "required": [
            "role",
            "content"
          ],
          "type": "object"
        },
        "Role": {
          "description": "Who produced a message.",
          "oneOf": [
            {
              "const": "user",
              "description": "The (real or simulated) user driving the skill.",
              "type": "string"
            },
            {
              "const": "assistant",
              "description": "The skill / assistant under test.",
              "type": "string"
            },
            {
              "const": "system",
              "description": "System-level framing, if a provider surfaces it.",
              "type": "string"
            }
          ]
        },
        "SkillPayload": {
          "additionalProperties": false,
          "properties": {
            "instructions": {
              "type": "string"
            },
            "name": {
              "type": "string"
            },
            "path": {
              "type": "string"
            }
          },
          "required": [
            "name",
            "path",
            "instructions"
          ],
          "type": "object"
        },
        "ToolEvent": {
          "description": "One normalized tool-call / action event the skill took during a turn, lifted\nfrom `oneharness`'s `events` array (its `--events` output). Harness-agnostic,\nso a consumer can inspect shell commands, file edits, and tool uses across any\nharness, not just the final text. `input` is the structured, tool-shaped args\nso a consumer can match on the command string or file path without re-parsing.\n\n`input` is free-form JSON, so `Message`/`Transcript` are `PartialEq` but not\n`Eq`.",
          "properties": {
            "index": {
              "default": 0,
              "description": "Position within the run, so ordering (\"did X before Y\") is expressible.",
              "format": "uint",
              "minimum": 0,
              "type": "integer"
            },
            "input": {
              "description": "Structured tool arguments (the command, the file path); `None` when none."
            },
            "kind": {
              "description": "`tool_call` (the skill invoked a tool) or `tool_result` (the observation).",
              "type": "string"
            },
            "name": {
              "description": "Normalized tool name where knowable (e.g. `bash`, `edit_file`); `None` for\na `tool_result` or when the harness did not name it.",
              "type": [
                "string",
                "null"
              ]
            },
            "output": {
              "description": "The result/observation text, when the transcript exposed it.",
              "type": [
                "string",
                "null"
              ]
            },
            "tool_call_id": {
              "description": "The harness's own stable identity for this call, joining a `tool_call` to\nits `tool_result`. `None` means the harness exposed no identity \u2014 never an\nempty string, which would read as an id that happens to be blank.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        }
      },
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "description": "`respond`: run one skill turn.",
      "properties": {
        "messages": {
          "items": {
            "$ref": "#/$defs/Message"
          },
          "type": "array"
        },
        "op": {
          "const": "respond",
          "description": "The operation this frame asks for.",
          "type": "string"
        },
        "session": {
          "type": [
            "string",
            "null"
          ]
        },
        "skill": {
          "$ref": "#/$defs/SkillPayload"
        }
      },
      "required": [
        "op",
        "skill",
        "messages"
      ],
      "title": "RespondFrame",
      "type": "object"
    },
    "agent.onejudge-frame.supervisor@7": {
      "$defs": {
        "Addressee": {
          "description": "Who a note is for.\n\nRequired; there is no default, because a note whose addressee is guessed is a\nnote the judge may read as an instruction to itself. One run saw a simulated\nuser compose a four-point \"manager ruling\" in-conversation and instruct the\nworker to post it over the run channel, and the worker complied.",
          "oneOf": [
            {
              "const": "worker",
              "description": "An update to the *worker's* task.",
              "type": "string"
            },
            {
              "const": "supervisor",
              "description": "An update to the *supervisor's* brief.",
              "type": "string"
            },
            {
              "const": "both",
              "description": "Addressed to both parties.",
              "type": "string"
            }
          ]
        },
        "Criterion": {
          "description": "The property a bound note requires of the finished work.\n\nA validated newtype, checked in the conversion that builds it, so a [`Note`]\ncarrying an unusable criterion is unrepresentable rather than\nrepresentable-and-refused-somewhere-later. The rules are the ones an\norchestrator already enforces on authored plan criteria\n(`orchestrator/criteria_guard.py` in `nickderobertis/ai-orchestrator`), ported\nhere so the *seam that accepts the note* refuses at the moment of binding\nrather than at render time \u2014 after the turn has been composed \u2014 or by the\njudge, which has already lost.\n\nWhat the rules cannot catch, stated plainly: \"this is a mechanism the author\npreferred rather than a property the node owes\" needs the author's intent, and\nno pattern has it. What stands in for it is the rendered framing\n([`Criteria::rendered`]), which instructs the judge to evaluate the property a\nnamed mechanism was serving.",
          "type": "string"
        },
        "DeliveredNote": {
          "description": "One note as it was handed to a party, with the party it reached.",
          "properties": {
            "delivered_to": {
              "$ref": "#/$defs/Party",
              "description": "The party that was handed it \u2014 **not** the same thing as its addressee: a\nnote is delivered to whoever is live, and the addressee is what the\nrecipient is told the note is *for*."
            },
            "note": {
              "$ref": "#/$defs/Note",
              "description": "The note itself, addressee included."
            }
          },
          "required": [
            "note",
            "delivered_to"
          ],
          "type": "object"
        },
        "Message": {
          "description": "A single turn in the conversation.",
          "properties": {
            "content": {
              "description": "The turn's text.",
              "type": "string"
            },
            "events": {
              "description": "The normalized tool events the skill took producing this turn (assistant\nturns only, and only when the harness exposed a tool transcript). Empty\notherwise. Surfaced for post-hoc analysis, streamed live, and rendered\ninto the transcript the judge sees.",
              "items": {
                "$ref": "#/$defs/ToolEvent"
              },
              "type": "array"
            },
            "role": {
              "$ref": "#/$defs/Role",
              "description": "Who produced the turn."
            }
          },
          "required": [
            "role",
            "content"
          ],
          "type": "object"
        },
        "Note": {
          "description": "One note: what the worker reads, who it is for, and the property it binds.\n\nBuilt through [`Note::new`] / [`Note::to`] / [`Note::binding`] and in no other\nway \u2014 `non_exhaustive` closes the struct literal, and deserialization goes\nthrough the same conversion \u2014 so a note whose text nobody can read, or whose\ncriterion is unusable, is unrepresentable rather than representable and refused\nsomewhere later.",
          "properties": {
            "addressee": {
              "$ref": "#/$defs/Addressee"
            },
            "criterion": {
              "anyOf": [
                {
                  "$ref": "#/$defs/Criterion"
                },
                {
                  "type": "null"
                }
              ],
              "default": null
            },
            "text": {
              "type": "string"
            }
          },
          "required": [
            "addressee",
            "text"
          ],
          "type": "object"
        },
        "Party": {
          "description": "Which party of the conversation a delivery reached.",
          "oneOf": [
            {
              "const": "worker",
              "description": "The agent under test.",
              "type": "string"
            },
            {
              "const": "supervisor",
              "description": "The simulated user / completion supervisor.",
              "type": "string"
            }
          ]
        },
        "Role": {
          "description": "Who produced a message.",
          "oneOf": [
            {
              "const": "user",
              "description": "The (real or simulated) user driving the skill.",
              "type": "string"
            },
            {
              "const": "assistant",
              "description": "The skill / assistant under test.",
              "type": "string"
            },
            {
              "const": "system",
              "description": "System-level framing, if a provider surfaces it.",
              "type": "string"
            }
          ]
        },
        "ToolEvent": {
          "description": "One normalized tool-call / action event the skill took during a turn, lifted\nfrom `oneharness`'s `events` array (its `--events` output). Harness-agnostic,\nso a consumer can inspect shell commands, file edits, and tool uses across any\nharness, not just the final text. `input` is the structured, tool-shaped args\nso a consumer can match on the command string or file path without re-parsing.\n\n`input` is free-form JSON, so `Message`/`Transcript` are `PartialEq` but not\n`Eq`.",
          "properties": {
            "index": {
              "default": 0,
              "description": "Position within the run, so ordering (\"did X before Y\") is expressible.",
              "format": "uint",
              "minimum": 0,
              "type": "integer"
            },
            "input": {
              "description": "Structured tool arguments (the command, the file path); `None` when none."
            },
            "kind": {
              "description": "`tool_call` (the skill invoked a tool) or `tool_result` (the observation).",
              "type": "string"
            },
            "name": {
              "description": "Normalized tool name where knowable (e.g. `bash`, `edit_file`); `None` for\na `tool_result` or when the harness did not name it.",
              "type": [
                "string",
                "null"
              ]
            },
            "output": {
              "description": "The result/observation text, when the transcript exposed it.",
              "type": [
                "string",
                "null"
              ]
            },
            "tool_call_id": {
              "description": "The harness's own stable identity for this call, joining a `tool_call` to\nits `tool_result`. `None` means the harness exposed no identity \u2014 never an\nempty string, which would read as an id that happens to be blank.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        }
      },
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "description": "`supervisor`: decide completion, or produce the next user turn.",
      "properties": {
        "done_when": {
          "type": [
            "string",
            "null"
          ]
        },
        "history_name": {
          "type": "string"
        },
        "messages": {
          "items": {
            "$ref": "#/$defs/Message"
          },
          "type": "array"
        },
        "notes": {
          "description": "Every note delivered into this run so far (protocol v5). Omitted when\nnone has been, so a v4 double sees a byte-identical request.",
          "items": {
            "$ref": "#/$defs/DeliveredNote"
          },
          "type": "array"
        },
        "op": {
          "const": "supervisor",
          "description": "The operation this frame asks for.",
          "type": "string"
        },
        "persona": {
          "type": "string"
        },
        "session": {
          "type": [
            "string",
            "null"
          ]
        },
        "task": {
          "type": "string"
        },
        "worktree": {
          "type": "string"
        }
      },
      "required": [
        "op",
        "task",
        "persona",
        "worktree",
        "history_name",
        "messages"
      ],
      "title": "SupervisorFrame",
      "type": "object"
    },
    "agent.onejudge-frame.user@7": {
      "$defs": {
        "Message": {
          "description": "A single turn in the conversation.",
          "properties": {
            "content": {
              "description": "The turn's text.",
              "type": "string"
            },
            "events": {
              "description": "The normalized tool events the skill took producing this turn (assistant\nturns only, and only when the harness exposed a tool transcript). Empty\notherwise. Surfaced for post-hoc analysis, streamed live, and rendered\ninto the transcript the judge sees.",
              "items": {
                "$ref": "#/$defs/ToolEvent"
              },
              "type": "array"
            },
            "role": {
              "$ref": "#/$defs/Role",
              "description": "Who produced the turn."
            }
          },
          "required": [
            "role",
            "content"
          ],
          "type": "object"
        },
        "Role": {
          "description": "Who produced a message.",
          "oneOf": [
            {
              "const": "user",
              "description": "The (real or simulated) user driving the skill.",
              "type": "string"
            },
            {
              "const": "assistant",
              "description": "The skill / assistant under test.",
              "type": "string"
            },
            {
              "const": "system",
              "description": "System-level framing, if a provider surfaces it.",
              "type": "string"
            }
          ]
        },
        "ToolEvent": {
          "description": "One normalized tool-call / action event the skill took during a turn, lifted\nfrom `oneharness`'s `events` array (its `--events` output). Harness-agnostic,\nso a consumer can inspect shell commands, file edits, and tool uses across any\nharness, not just the final text. `input` is the structured, tool-shaped args\nso a consumer can match on the command string or file path without re-parsing.\n\n`input` is free-form JSON, so `Message`/`Transcript` are `PartialEq` but not\n`Eq`.",
          "properties": {
            "index": {
              "default": 0,
              "description": "Position within the run, so ordering (\"did X before Y\") is expressible.",
              "format": "uint",
              "minimum": 0,
              "type": "integer"
            },
            "input": {
              "description": "Structured tool arguments (the command, the file path); `None` when none."
            },
            "kind": {
              "description": "`tool_call` (the skill invoked a tool) or `tool_result` (the observation).",
              "type": "string"
            },
            "name": {
              "description": "Normalized tool name where knowable (e.g. `bash`, `edit_file`); `None` for\na `tool_result` or when the harness did not name it.",
              "type": [
                "string",
                "null"
              ]
            },
            "output": {
              "description": "The result/observation text, when the transcript exposed it.",
              "type": [
                "string",
                "null"
              ]
            },
            "tool_call_id": {
              "description": "The harness's own stable identity for this call, joining a `tool_call` to\nits `tool_result`. `None` means the harness exposed no identity \u2014 never an\nempty string, which would read as an id that happens to be blank.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        }
      },
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "description": "`user`: produce one simulated-user turn.",
      "properties": {
        "messages": {
          "items": {
            "$ref": "#/$defs/Message"
          },
          "type": "array"
        },
        "op": {
          "const": "user",
          "description": "The operation this frame asks for.",
          "type": "string"
        },
        "persona": {
          "type": "string"
        },
        "session": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "op",
        "persona",
        "messages"
      ],
      "title": "UserFrame",
      "type": "object"
    }
  },
  "observation": {
    "$defs": {
      "Decision": {
        "description": "What one judge of a [`JudgePanel`](crate::JudgePanel) decided, as a wire token.\n\nThe five outcomes a supervisor call can have, plus `error` for a judge that\ncould not run \u2014 recorded rather than dropped, because a judge that could not\nrun is never read as a pass and a reader has to be able to see that it was\nasked.",
        "oneOf": [
          {
            "const": "done",
            "description": "The judge declared the work complete.",
            "type": "string"
          },
          {
            "const": "continue",
            "description": "The judge handed back a next instruction.",
            "type": "string"
          },
          {
            "const": "no_instruction",
            "description": "The judge judged the work incomplete but named no next instruction, even\nasked again.",
            "type": "string"
          },
          {
            "const": "unparseable",
            "description": "The judge answered in neither documented shape, even asked again.",
            "type": "string"
          },
          {
            "const": "error",
            "description": "The judge's call failed; `reason` is the error's message.",
            "type": "string"
          }
        ]
      },
      "JudgeDecided": {
        "description": "One judge's decision on a supervisor turn, as its panel recorded it.\n\nThe observed form of [`JudgeDecision`](crate::JudgeDecision), stamped with the\nturn it belongs to. Only a [`JudgePanel`](crate::JudgePanel) produces these; a\nrun judged by a bare provider is observed exactly as before.",
        "properties": {
          "decision": {
            "$ref": "#/$defs/Decision",
            "description": "What it decided, or that it failed."
          },
          "judge": {
            "description": "The judge's label within its panel.",
            "type": "string"
          },
          "kind": {
            "description": "The judge entry's provider kind.",
            "type": "string"
          },
          "reason": {
            "description": "Its own reason \u2014 or, for an `error`, the error's message.",
            "type": "string"
          },
          "turn": {
            "description": "1-based assistant-turn index the supervisor turn belongs to, as\n[`TurnOpened::turn`].",
            "format": "uint",
            "minimum": 0,
            "type": "integer"
          }
        },
        "required": [
          "turn",
          "judge",
          "kind",
          "decision",
          "reason"
        ],
        "type": "object"
      },
      "Role": {
        "description": "Who produced a message.",
        "oneOf": [
          {
            "const": "user",
            "description": "The (real or simulated) user driving the skill.",
            "type": "string"
          },
          {
            "const": "assistant",
            "description": "The skill / assistant under test.",
            "type": "string"
          },
          {
            "const": "system",
            "description": "System-level framing, if a provider surfaces it.",
            "type": "string"
          }
        ]
      },
      "StreamEvent": {
        "description": "One streamed tool event delivered live to an [`Engine::run_streaming`] sink,\ntagged with the turn it belongs to.\n\nIt is also the payload of an `event` line in the **streamed protocol**\n(`docs/streaming.md`) that `onejudge run --stream` publishes \u2014 the line adds\nthe `\"type\": \"event\"` discriminator around exactly these fields, so an SDK\nhands this type straight to its own event callback.",
        "properties": {
          "event": {
            "$ref": "#/$defs/ToolEvent",
            "description": "The normalized tool event."
          },
          "turn": {
            "description": "1-based assistant-turn index within this run.",
            "format": "uint",
            "minimum": 0,
            "type": "integer"
          }
        },
        "required": [
          "turn",
          "event"
        ],
        "type": "object"
      },
      "ToolEvent": {
        "description": "One normalized tool-call / action event the skill took during a turn, lifted\nfrom `oneharness`'s `events` array (its `--events` output). Harness-agnostic,\nso a consumer can inspect shell commands, file edits, and tool uses across any\nharness, not just the final text. `input` is the structured, tool-shaped args\nso a consumer can match on the command string or file path without re-parsing.\n\n`input` is free-form JSON, so `Message`/`Transcript` are `PartialEq` but not\n`Eq`.",
        "properties": {
          "index": {
            "default": 0,
            "description": "Position within the run, so ordering (\"did X before Y\") is expressible.",
            "format": "uint",
            "minimum": 0,
            "type": "integer"
          },
          "input": {
            "description": "Structured tool arguments (the command, the file path); `None` when none."
          },
          "kind": {
            "description": "`tool_call` (the skill invoked a tool) or `tool_result` (the observation).",
            "type": "string"
          },
          "name": {
            "description": "Normalized tool name where knowable (e.g. `bash`, `edit_file`); `None` for\na `tool_result` or when the harness did not name it.",
            "type": [
              "string",
              "null"
            ]
          },
          "output": {
            "description": "The result/observation text, when the transcript exposed it.",
            "type": [
              "string",
              "null"
            ]
          },
          "tool_call_id": {
            "description": "The harness's own stable identity for this call, joining a `tool_call` to\nits `tool_result`. `None` means the harness exposed no identity \u2014 never an\nempty string, which would read as an id that happens to be blank.",
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "kind",
          "index"
        ],
        "type": "object"
      },
      "TurnClosed": {
        "description": "A turn ending, with what it cost and the interval it ran over.",
        "properties": {
          "finished_at": {
            "description": "When the turn ended, RFC 3339 with millisecond precision in UTC.",
            "type": "string"
          },
          "role": {
            "$ref": "#/$defs/Role",
            "description": "Who spoke."
          },
          "started_at": {
            "description": "When the turn began, RFC 3339 with millisecond precision in UTC \u2014 the same\nvalue the matching [`TurnOpened`] carried.",
            "type": "string"
          },
          "turn": {
            "description": "1-based assistant-turn index within this run, as [`TurnOpened::turn`].",
            "format": "uint",
            "minimum": 0,
            "type": "integer"
          },
          "usage": {
            "anyOf": [
              {
                "$ref": "#/$defs/Usage"
              },
              {
                "type": "null"
              }
            ],
            "description": "This one turn's usage. Absent means the provider reported none, which is a\ndifferent fact from a turn that cost nothing."
          }
        },
        "required": [
          "turn",
          "role",
          "started_at",
          "finished_at"
        ],
        "type": "object"
      },
      "TurnMessage": {
        "description": "A party's own words for one turn, observed as they are appended to the\ntranscript.\n\nA supervisor turn that completes or settles the run appends nothing, so it is\nobserved as an opening and a closing with no message between them.",
        "properties": {
          "role": {
            "$ref": "#/$defs/Role",
            "description": "Who is speaking."
          },
          "text": {
            "description": "That party's own words for this turn, whole and unbounded.",
            "type": "string"
          },
          "turn": {
            "description": "1-based assistant-turn index within this run, as [`TurnOpened::turn`].",
            "format": "uint",
            "minimum": 0,
            "type": "integer"
          }
        },
        "required": [
          "turn",
          "role",
          "text"
        ],
        "type": "object"
      },
      "TurnOpened": {
        "description": "A turn beginning, and the message it was given to answer.\n\nThe conversation's opening input is never a [`TurnMessage`] \u2014 it is already in\nthe transcript before the loop runs \u2014 so it reaches an observer here, as the\n`instruction` of the first assistant turn.",
        "properties": {
          "instruction": {
            "description": "The message this turn answers, whole and unbounded \u2014 the original task on\nthe first assistant turn, the supervisor's last instruction afterwards, and\nthe agent's last reply for a user turn.",
            "type": "string"
          },
          "role": {
            "$ref": "#/$defs/Role",
            "description": "Which party is about to speak."
          },
          "started_at": {
            "description": "When the turn began, RFC 3339 with millisecond precision in UTC.",
            "type": "string"
          },
          "turn": {
            "description": "1-based assistant-turn index within this run, as [`StreamEvent::turn`]. A\nuser turn carries the index of the assistant turn it is reacting to.",
            "format": "uint",
            "minimum": 0,
            "type": "integer"
          }
        },
        "required": [
          "turn",
          "role",
          "instruction",
          "started_at"
        ],
        "type": "object"
      },
      "Usage": {
        "description": "Token / cost usage for one provider call, or a running total.\n\nEach field is independently optional because not every harness reports every\nsignal (cost is commonly absent on subscription auth, and cache counts only\nwhen the harness surfaces prompt-cache reads/writes). `None` means \"no\nsignal\", never \"zero\" \u2014 so a total stays `None` until something reports a real\nnumber, then accumulates.",
        "properties": {
          "cache_read_tokens": {
            "description": "Prompt tokens served from the provider's prompt cache \u2014 a cheap read of a\npreviously-written prefix \u2014 when the harness reports them. `None` when the\nharness does not surface cache counts, never `0` as a guess.",
            "format": "uint64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "cache_write_tokens": {
            "description": "Prompt tokens written to the provider's prompt cache (a.k.a. cache\ncreation), when the harness reports them. `None` when not surfaced.",
            "format": "uint64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "cost_usd": {
            "description": "Total cost in USD, when reported.",
            "format": "double",
            "type": [
              "number",
              "null"
            ]
          },
          "input_tokens": {
            "description": "Prompt/input tokens billed, when reported.",
            "format": "uint64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "output_tokens": {
            "description": "Completion/output tokens billed, when reported.",
            "format": "uint64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          }
        },
        "type": "object"
      }
    },
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "description": "One live observation of the conversation as the engine produces it, delivered\nto an [`Engine::run_observing`] sink.\n\nThe wider seam beside [`StreamEvent`], not a replacement for it: a tool event\nstill reaches a sink as [`Observation::Tool`] carrying exactly the same\npayload, and [`Engine::run_streaming`] still delivers only those. What the\nwider sink adds is the prose an operator watching a dispatch they cannot see\notherwise reads it by \u2014 the instruction each turn is answering, each party's\nown reply, and the turn's own cost and bounds.\n\nThe engine bounds nothing it hands over. `instruction` and `text` are borrows\nof the whole message; where a payload has to be bounded, that bound belongs to\nwhatever journal or transport the embedder writes it to, which is the only\nlayer that knows what its own limit is.\n\nReturning [`ControlFlow::Break`] means here exactly what it means on the\nstreaming sink: the run stops, [`Outcome::stopped_early`] is `true`, and no\nfurther observation is delivered.",
    "oneOf": [
      {
        "$ref": "#/$defs/TurnOpened",
        "description": "A turn began, naming the message it is answering.",
        "properties": {
          "type": {
            "const": "turn_opened",
            "type": "string"
          }
        },
        "required": [
          "type"
        ],
        "type": "object"
      },
      {
        "$ref": "#/$defs/StreamEvent",
        "description": "A tool event, exactly as [`Engine::run_streaming`] delivers it.",
        "properties": {
          "type": {
            "const": "tool",
            "type": "string"
          }
        },
        "required": [
          "type"
        ],
        "type": "object"
      },
      {
        "$ref": "#/$defs/TurnMessage",
        "description": "A party's own words for a turn, as they are appended to the transcript.",
        "properties": {
          "type": {
            "const": "message",
            "type": "string"
          }
        },
        "required": [
          "type"
        ],
        "type": "object"
      },
      {
        "$ref": "#/$defs/TurnClosed",
        "description": "A turn ended, with what it cost and when it ran.",
        "properties": {
          "type": {
            "const": "turn_closed",
            "type": "string"
          }
        },
        "required": [
          "type"
        ],
        "type": "object"
      },
      {
        "$ref": "#/$defs/JudgeDecided",
        "description": "One judge of a panel decided, on a supervisor turn: delivered once per\ndecision in the panel's list order, after that turn's\n[`TurnOpened`](Observation::TurnOpened) and before its\n[`Message`](Observation::Message) / [`TurnClosed`](Observation::TurnClosed)\n\u2014 and, when the supervisor call failed, before the error propagates.",
        "properties": {
          "type": {
            "const": "judge_decided",
            "type": "string"
          }
        },
        "required": [
          "type"
        ],
        "type": "object"
      }
    ],
    "title": "Observation"
  },
  "report": {
    "$defs": {
      "CandidateAttempt": {
        "description": "One candidate identity oneharness attempted for a single invocation \u2014 the\nharness (and variant/model) it tried, and what came of it.\n\nUnder `run_mode = \"fallback\"` oneharness attempts candidates in priority order\nand stops at the first that runs the task, so an invocation can have several of\nthese: the ones it fell through, then the one that ran. This is the typed form\nof oneharness's per-candidate record \u2014 the thing that lets a consumer attribute\na failure to a *side* (agent vs judge) and an *identity* (which harness, which\naccount) instead of grepping a message.",
        "properties": {
          "available": {
            "description": "Whether oneharness found the harness binary.",
            "type": "boolean"
          },
          "duration_ms": {
            "description": "Wall-clock duration of this attempt.",
            "format": "uint64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "error": {
            "description": "oneharness's human-readable problem statement for a failed attempt.",
            "type": [
              "string",
              "null"
            ]
          },
          "exit_code": {
            "description": "The harness process's exit code, when it ran to completion.",
            "format": "int32",
            "type": [
              "integer",
              "null"
            ]
          },
          "failure_kind": {
            "description": "oneharness's normalized failure reason (`auth`, `rate_limit`,\n`model_not_found`, `quota`, `tool_deferred`, `session_not_found`,\n`untrusted_directory`, `input_too_large`, `model_mismatch`), when it\nclassified one.",
            "type": [
              "string",
              "null"
            ]
          },
          "failure_kind_source": {
            "description": "Where that reason was read (`stderr`, `stdout`, `config:env_from`).",
            "type": [
              "string",
              "null"
            ]
          },
          "harness": {
            "description": "Canonical harness id (e.g. `claude-code`).",
            "type": "string"
          },
          "harness_id": {
            "description": "Base id, or `<base>:<variant>` when a named preset selected the identity \u2014\nthe selector that reproduces this candidate.",
            "type": "string"
          },
          "history_id": {
            "description": "The oneharness history record written for this attempt, when history was\non \u2014 the handle `oneharness history show` resolves.",
            "type": [
              "string",
              "null"
            ]
          },
          "model": {
            "description": "The model this candidate ran with, when one was requested.",
            "type": [
              "string",
              "null"
            ]
          },
          "ran": {
            "description": "Whether this is the candidate that actually ran the turn.",
            "type": "boolean"
          },
          "session_id": {
            "description": "The harness's own continuation id, when it exposed one.",
            "type": [
              "string",
              "null"
            ]
          },
          "status": {
            "description": "oneharness's status token: `ok`, `nonzero`, `timeout`, `spawn-error`,\n`skipped`, or `planned`.",
            "type": "string"
          },
          "usage": {
            "anyOf": [
              {
                "$ref": "#/$defs/Usage"
              },
              {
                "type": "null"
              }
            ],
            "description": "This attempt's own token/cost accounting."
          },
          "variant": {
            "description": "The named preset, when this candidate came from a composed harness id.",
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "harness",
          "harness_id",
          "status",
          "available",
          "ran"
        ],
        "type": "object"
      },
      "ControlAddress": {
        "description": "Where an `oneharness interrupt` process addresses the controllable turn\nonejudge's agent side opened.\n\nThe three fields are the whole of `interrupt`'s addressing:\n\n```text\noneharness interrupt --session <session> --session-dir <session_dir> \\\n                     --cwd <cwd> --input \"<what to do instead>\"\n```\n\nThey are read back from the oneharness run report rather than re-derived from\nwhat onejudge asked for, so the address names the session the run actually\nbound \u2014 including in a fallback chain, where the handle is bound to the\ncandidate that *ran*, not the first one tried.",
        "properties": {
          "cwd": {
            "description": "The working directory the turn runs in, which is how `interrupt` resolves\n*which project's* session the handle belongs to (`interrupt --cwd`). It is\nthe same string onejudge passed as `oneharness run --cwd`, because\noneharness slugs that string to key the store.",
            "type": "string"
          },
          "session": {
            "description": "The caller-owned session handle the turn is addressed by, as oneharness\nsanitized and stored it (`interrupt --session`).",
            "type": "string"
          },
          "session_dir": {
            "description": "The session store directory holding both the handle and its\n`control/<session>.sock` (`interrupt --session-dir`). Absolute: the\naddress is handed to a different process, so a relative path would name a\ndifferent directory depending on who read it.",
            "type": "string"
          }
        },
        "required": [
          "session",
          "session_dir",
          "cwd"
        ],
        "type": "object"
      },
      "Decision": {
        "description": "What one judge of a [`JudgePanel`](crate::JudgePanel) decided, as a wire token.\n\nThe five outcomes a supervisor call can have, plus `error` for a judge that\ncould not run \u2014 recorded rather than dropped, because a judge that could not\nrun is never read as a pass and a reader has to be able to see that it was\nasked.",
        "oneOf": [
          {
            "const": "done",
            "description": "The judge declared the work complete.",
            "type": "string"
          },
          {
            "const": "continue",
            "description": "The judge handed back a next instruction.",
            "type": "string"
          },
          {
            "const": "no_instruction",
            "description": "The judge judged the work incomplete but named no next instruction, even\nasked again.",
            "type": "string"
          },
          {
            "const": "unparseable",
            "description": "The judge answered in neither documented shape, even asked again.",
            "type": "string"
          },
          {
            "const": "error",
            "description": "The judge's call failed; `reason` is the error's message.",
            "type": "string"
          }
        ]
      },
      "FellThrough": {
        "description": "One candidate a fallback chain fell through, with oneharness's reason token\n(`not-installed`, `spawn-error`, `auth`, `quota`, `model-not-found`,\n`rate-limit`, `session-not-found`, `untrusted-directory`, `input-too-large`,\n`model-mismatch`).",
        "properties": {
          "harness": {
            "description": "Canonical harness id.",
            "type": "string"
          },
          "reason": {
            "description": "Why it could not run the task at all.",
            "type": "string"
          }
        },
        "required": [
          "harness",
          "reason"
        ],
        "type": "object"
      },
      "HarnessAttribution": {
        "description": "What oneharness attempted for one provider invocation, placed on the side and\nturn that made the call.",
        "properties": {
          "candidates": {
            "description": "Every attempted candidate, in the order oneharness tried them.",
            "items": {
              "$ref": "#/$defs/CandidateAttempt"
            },
            "type": "array"
          },
          "fell_through": {
            "description": "Candidates a fallback chain routed around, in priority order.",
            "items": {
              "$ref": "#/$defs/FellThrough"
            },
            "type": "array"
          },
          "history_file": {
            "description": "The oneharness history session file this invocation appended to.",
            "type": [
              "string",
              "null"
            ]
          },
          "judge": {
            "description": "Which judge of a [`JudgePanel`](crate::JudgePanel) made the invocation, by\nlabel \u2014 as [`SessionLink::judge`], and absent under the same conditions.",
            "type": [
              "string",
              "null"
            ]
          },
          "ran": {
            "description": "The candidate that ran the turn, by composed id; `null` when none could.",
            "type": [
              "string",
              "null"
            ]
          },
          "role": {
            "$ref": "#/$defs/TelemetryRole",
            "description": "Which side of the conversation made this call."
          },
          "turn_index": {
            "description": "One-based invocation index within the role.",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          }
        },
        "required": [
          "role",
          "turn_index",
          "candidates"
        ],
        "type": "object"
      },
      "JudgeDecision": {
        "description": "One judge's decision on one judge-side call, attributed to the judge by its\npanel label and provider kind.\n\nA [`JudgePanel`](crate::JudgePanel) records one per judge per supervisor call\n\u2014 including the call that failed \u2014 and the engine drains them after every\nsupervisor call, `Ok` or `Err`, onto [`Report::judge_decisions`]. A provider\nthat is not a panel records none, so a report carrying no decisions was judged\nby a bare provider; nothing is synthesized.",
        "properties": {
          "decision": {
            "$ref": "#/$defs/Decision",
            "description": "What it decided, or that it failed."
          },
          "judge": {
            "description": "The judge's label within its panel (`label:` in the config, or the\ndefaulted `<kind>` / `<kind>-<n>`).",
            "type": "string"
          },
          "kind": {
            "description": "The judge entry's provider kind (`oneharness`, `command`, \u2026).",
            "type": "string"
          },
          "reason": {
            "description": "Its own reason \u2014 or, for an `error`, the error's message.",
            "type": "string"
          }
        },
        "required": [
          "judge",
          "kind",
          "decision",
          "reason"
        ],
        "type": "object"
      },
      "JudgeKind": {
        "description": "The kind of judgement requested.",
        "oneOf": [
          {
            "const": "boolean",
            "description": "A yes/no verdict.",
            "type": "string"
          },
          {
            "const": "numeric",
            "description": "A score on a `[min, max]` scale.",
            "type": "string"
          }
        ]
      },
      "JudgeValue": {
        "anyOf": [
          {
            "description": "A boolean verdict.",
            "type": "boolean"
          },
          {
            "description": "A numeric score.",
            "format": "double",
            "type": "number"
          }
        ],
        "description": "The raw value a judge returns: a boolean or a number, matching the query kind.\nDeserialized untagged from the provider's `value` field."
      },
      "JudgeVerdict": {
        "description": "A judge verdict: the raw value (bool or number) plus the stated reason. Part\nof onejudge's versioned [`Report`](crate::Report) contract, so it round-trips\nthrough serde.",
        "properties": {
          "reason": {
            "default": "",
            "description": "The judge's one-sentence justification.",
            "type": "string"
          },
          "usage": {
            "anyOf": [
              {
                "$ref": "#/$defs/Usage"
              },
              {
                "type": "null"
              }
            ],
            "description": "Cost/token usage for the judge call, if reported."
          },
          "value": {
            "$ref": "#/$defs/JudgeValue",
            "description": "The parsed verdict value."
          }
        },
        "required": [
          "value",
          "reason"
        ],
        "type": "object"
      },
      "JudgedTurn": {
        "description": "Every judge's decision on one supervisor turn.",
        "properties": {
          "decisions": {
            "description": "One decision per judge, in the panel's list order.",
            "items": {
              "$ref": "#/$defs/JudgeDecision"
            },
            "type": "array"
          },
          "turn": {
            "description": "1-based assistant-turn index the supervisor turn belongs to, as\n[`TurnOpened::turn`](crate::TurnOpened::turn).",
            "format": "uint",
            "minimum": 0,
            "type": "integer"
          }
        },
        "required": [
          "turn",
          "decisions"
        ],
        "type": "object"
      },
      "Message": {
        "description": "A single turn in the conversation.",
        "properties": {
          "content": {
            "description": "The turn's text.",
            "type": "string"
          },
          "events": {
            "description": "The normalized tool events the skill took producing this turn (assistant\nturns only, and only when the harness exposed a tool transcript). Empty\notherwise. Surfaced for post-hoc analysis, streamed live, and rendered\ninto the transcript the judge sees.",
            "items": {
              "$ref": "#/$defs/ToolEvent"
            },
            "type": "array"
          },
          "role": {
            "$ref": "#/$defs/Role",
            "description": "Who produced the turn."
          }
        },
        "required": [
          "role",
          "content"
        ],
        "type": "object"
      },
      "NamedVerdict": {
        "description": "A judge verdict paired with the criterion it scored and the kind of\njudgement, so a serialized report is self-describing.",
        "properties": {
          "criterion": {
            "description": "The plain-English criterion that was scored.",
            "type": "string"
          },
          "kind": {
            "$ref": "#/$defs/JudgeKind",
            "description": "Whether it was a boolean or numeric judgement."
          },
          "verdict": {
            "$ref": "#/$defs/JudgeVerdict",
            "description": "The verdict itself (value, reason, and per-call usage)."
          }
        },
        "required": [
          "criterion",
          "kind",
          "verdict"
        ],
        "type": "object"
      },
      "PartyTelemetry": {
        "description": "Aggregated measurements for one side of a run.",
        "properties": {
          "model_ms": {
            "description": "Model/provider elapsed time, when every contributing invocation reported it.",
            "format": "uint64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "session_ids": {
            "description": "Native oneharness session IDs in first-seen order, without duplicates.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "time_to_first_token_ms": {
            "description": "Time to first token from the first invocation that reported it.",
            "format": "uint64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "tool_ms": {
            "description": "Tool elapsed time, when every contributing invocation reported it.",
            "format": "uint64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "usage": {
            "anyOf": [
              {
                "$ref": "#/$defs/Usage"
              },
              {
                "type": "null"
              }
            ],
            "description": "Strictly aggregated usage; each field is present only when every invocation knew it."
          }
        },
        "type": "object"
      },
      "Role": {
        "description": "Who produced a message.",
        "oneOf": [
          {
            "const": "user",
            "description": "The (real or simulated) user driving the skill.",
            "type": "string"
          },
          {
            "const": "assistant",
            "description": "The skill / assistant under test.",
            "type": "string"
          },
          {
            "const": "system",
            "description": "System-level framing, if a provider surfaces it.",
            "type": "string"
          }
        ]
      },
      "SessionLink": {
        "description": "Authoritative linkage from a onejudge role/turn to a native oneharness run.",
        "properties": {
          "finished_at": {
            "description": "Upstream UTC invocation-finish timestamp, or `null` when not observed.",
            "type": [
              "string",
              "null"
            ]
          },
          "history_id": {
            "description": "Native oneharness history record identity, when history was available.",
            "type": [
              "string",
              "null"
            ]
          },
          "judge": {
            "description": "Which judge of a [`JudgePanel`](crate::JudgePanel) made the invocation, by\nlabel. Set only by a panel holding more than one judge; absent for a bare\nprovider and for a panel of one, whose records are the ones it has always\nwritten.",
            "type": [
              "string",
              "null"
            ]
          },
          "role": {
            "$ref": "#/$defs/TelemetryRole",
            "description": "Which side of the conversation owns the invocation."
          },
          "session_id": {
            "description": "Native harness continuation identifier.",
            "type": "string"
          },
          "started_at": {
            "description": "Upstream UTC invocation-start timestamp.",
            "type": "string"
          },
          "turn_index": {
            "description": "One-based invocation index within the role.",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          }
        },
        "required": [
          "session_id",
          "role",
          "turn_index",
          "started_at",
          "finished_at"
        ],
        "type": "object"
      },
      "SpawnedProcess": {
        "description": "One process onejudge spawned for a run, and the group an embedder's\n[`SpawnHook`] placed it in.\n\nCarried on [`Report::processes`](crate::Report::processes) (and on the CLI's\n`--format json` failure document), so what an in-process embedder can observe\nthrough the hook is also machine-readable from the command line.\n\n`group` is present **only** when a hook named one: onejudge never reports a\ngroup that does not exist, so a `null` group is the honest statement that this\nprocess is in whatever group onejudge itself was in.",
        "properties": {
          "group": {
            "description": "The embedder-owned group a [`SpawnHook`] reported placing it in.",
            "type": [
              "string",
              "null"
            ]
          },
          "judge": {
            "description": "Which judge of a [`JudgePanel`](crate::JudgePanel) spawned it, by label.\nSet only by a panel holding more than one judge; absent otherwise, so a\nsingle-judge run's records are the ones it has always written.",
            "type": [
              "string",
              "null"
            ]
          },
          "op": {
            "description": "The provider operation that asked for it.",
            "type": "string"
          },
          "pid": {
            "description": "The operating-system process id it was given.",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "program": {
            "description": "The program that was run (`argv[0]`).",
            "type": "string"
          },
          "role": {
            "$ref": "#/$defs/TelemetryRole",
            "description": "Which side of the conversation the process served."
          }
        },
        "required": [
          "role",
          "op",
          "program",
          "pid"
        ],
        "type": "object"
      },
      "Telemetry": {
        "description": "Timing, usage, and native linkage for the complete agent+judge run.",
        "properties": {
          "agent": {
            "$ref": "#/$defs/PartyTelemetry",
            "description": "Agent-side measurements."
          },
          "attribution": {
            "description": "Which harness identities each invocation attempted, in invocation order.\nEmpty when the provider reports no candidate identities.",
            "items": {
              "$ref": "#/$defs/HarnessAttribution"
            },
            "type": "array"
          },
          "judge": {
            "$ref": "#/$defs/PartyTelemetry",
            "description": "Simulated-user, supervisor, and evaluator measurements."
          },
          "orchestration_ms": {
            "description": "Non-negative wall-time remainder after measured model and tool work.",
            "format": "uint64",
            "minimum": 0,
            "type": "integer"
          },
          "sessions": {
            "default": [],
            "description": "Native session linkage records in invocation order.",
            "items": {
              "$ref": "#/$defs/SessionLink"
            },
            "type": "array"
          },
          "wall_ms": {
            "description": "Monotonic duration from task-loop entry through final result assembly.",
            "format": "uint64",
            "minimum": 0,
            "type": "integer"
          }
        },
        "required": [
          "wall_ms",
          "agent",
          "judge",
          "orchestration_ms",
          "sessions"
        ],
        "type": "object"
      },
      "TelemetryRole": {
        "description": "The side of the evaluated conversation that owns an invocation.",
        "oneOf": [
          {
            "const": "agent",
            "description": "The skill/agent under evaluation.",
            "type": "string"
          },
          {
            "const": "judge",
            "description": "The simulated user, supervisor, or final evaluator.",
            "type": "string"
          }
        ]
      },
      "ToolEvent": {
        "description": "One normalized tool-call / action event the skill took during a turn, lifted\nfrom `oneharness`'s `events` array (its `--events` output). Harness-agnostic,\nso a consumer can inspect shell commands, file edits, and tool uses across any\nharness, not just the final text. `input` is the structured, tool-shaped args\nso a consumer can match on the command string or file path without re-parsing.\n\n`input` is free-form JSON, so `Message`/`Transcript` are `PartialEq` but not\n`Eq`.",
        "properties": {
          "index": {
            "default": 0,
            "description": "Position within the run, so ordering (\"did X before Y\") is expressible.",
            "format": "uint",
            "minimum": 0,
            "type": "integer"
          },
          "input": {
            "description": "Structured tool arguments (the command, the file path); `None` when none."
          },
          "kind": {
            "description": "`tool_call` (the skill invoked a tool) or `tool_result` (the observation).",
            "type": "string"
          },
          "name": {
            "description": "Normalized tool name where knowable (e.g. `bash`, `edit_file`); `None` for\na `tool_result` or when the harness did not name it.",
            "type": [
              "string",
              "null"
            ]
          },
          "output": {
            "description": "The result/observation text, when the transcript exposed it.",
            "type": [
              "string",
              "null"
            ]
          },
          "tool_call_id": {
            "description": "The harness's own stable identity for this call, joining a `tool_call` to\nits `tool_result`. `None` means the harness exposed no identity \u2014 never an\nempty string, which would read as an id that happens to be blank.",
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "kind",
          "index"
        ],
        "type": "object"
      },
      "Transcript": {
        "description": "An ordered list of messages.",
        "properties": {
          "messages": {
            "description": "The turns in order, oldest first.",
            "items": {
              "$ref": "#/$defs/Message"
            },
            "type": "array"
          }
        },
        "required": [
          "messages"
        ],
        "type": "object"
      },
      "Usage": {
        "description": "Token / cost usage for one provider call, or a running total.\n\nEach field is independently optional because not every harness reports every\nsignal (cost is commonly absent on subscription auth, and cache counts only\nwhen the harness surfaces prompt-cache reads/writes). `None` means \"no\nsignal\", never \"zero\" \u2014 so a total stays `None` until something reports a real\nnumber, then accumulates.",
        "properties": {
          "cache_read_tokens": {
            "description": "Prompt tokens served from the provider's prompt cache \u2014 a cheap read of a\npreviously-written prefix \u2014 when the harness reports them. `None` when the\nharness does not surface cache counts, never `0` as a guess.",
            "format": "uint64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "cache_write_tokens": {
            "description": "Prompt tokens written to the provider's prompt cache (a.k.a. cache\ncreation), when the harness reports them. `None` when not surfaced.",
            "format": "uint64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "cost_usd": {
            "description": "Total cost in USD, when reported.",
            "format": "double",
            "type": [
              "number",
              "null"
            ]
          },
          "input_tokens": {
            "description": "Prompt/input tokens billed, when reported.",
            "format": "uint64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "output_tokens": {
            "description": "Completion/output tokens billed, when reported.",
            "format": "uint64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          }
        },
        "type": "object"
      }
    },
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "description": "A judged run: the transcript, the verdicts scored against it, aggregated\nusage, and whether a streaming sink short-circuited the run \u2014 stamped with the\n[`SCHEMA_VERSION`] of the contract that produced it.\n\nBuild one from an [`Outcome`](crate::Outcome) with\n[`Outcome::into_report`](crate::Outcome::into_report), or directly with\n[`Report::new`].",
    "properties": {
      "assessment": {
        "description": "A free-text judgement requested by the caller, if any.",
        "type": [
          "string",
          "null"
        ]
      },
      "completion_reason": {
        "description": "Why the per-turn supervisor declared the task complete, if it did.",
        "type": [
          "string",
          "null"
        ]
      },
      "control": {
        "anyOf": [
          {
            "$ref": "#/$defs/ControlAddress"
          },
          {
            "type": "null"
          }
        ],
        "default": null,
        "description": "Where an `oneharness interrupt` process addresses the controllable turn\nthis run's agent side opened (`provider.control: true`), or `null` when\ncontrol was not asked for.\n\nSerialized even when null \u2014 unlike the optional fields above \u2014 because a\nsupervisor keys on its presence to decide whether it has a lever at all,\nand an absent key would make \"no control\" indistinguishable from a report\nwritten before this field existed. `null` with a `control_unavailable`\nreason beside it is the third case: asked for, and refused.\n\nAlways *written*, but deliberately not *required* by the generated schema:\na reader must keep accepting the reports older onejudge versions wrote, and\nproducing more than the contract demands is what makes an additive field\nadditive. [`never`] is how those two are said at once \u2014 it is the skip\npredicate that never skips, and its presence is what tells serde's schema\nderive the key may be absent from a document."
      },
      "control_unavailable": {
        "description": "Why an *asked-for* control channel is missing, when one is. Absent unless\n`provider.control` was on and the run could not be given a socket \u2014 which\nis what keeps a refused ask from reading as an ask never made.",
        "type": [
          "string",
          "null"
        ]
      },
      "judge_decisions": {
        "description": "What each judge of a panel decided on each supervisor turn, one entry per\nturn that consulted the supervisor, in turn order. Omitted when empty \u2014 a\nrun judged by a bare provider rather than a [`JudgePanel`](crate::JudgePanel)\nrecords no per-judge decision, and nothing is synthesized for it.",
        "items": {
          "$ref": "#/$defs/JudgedTurn"
        },
        "type": "array"
      },
      "processes": {
        "description": "Every process the run spawned, and the embedder-owned group a\n[`SpawnHook`](crate::SpawnHook) placed each one in.\n\nThis is the machine-readable form of what an in-process embedder observes\nthrough the hook, so `onejudge run --format json` reports it too. A record\nwhose `group` is absent is not grouped \u2014 onejudge never names a group it did\nnot observe a hook create.",
        "items": {
          "$ref": "#/$defs/SpawnedProcess"
        },
        "type": "array"
      },
      "schema_version": {
        "description": "The contract version this report was serialized under.",
        "format": "uint32",
        "minimum": 0,
        "type": "integer"
      },
      "settled_reason": {
        "description": "Why the run ended *without* a completion decision, when it ended because the\nsupervisor judged the work incomplete and then named no next instruction \u2014\neven asked again.\n\nAbsent on every other run, so its presence is the one signal that separates\n\"the agent could not do it\" from \"the supervisor had nothing to say\". The\ntranscript, verdicts, and usage beside it are the work the run really did:\nthis settles a run, it does not fail one.",
        "type": [
          "string",
          "null"
        ]
      },
      "stopped_early": {
        "default": false,
        "description": "Whether a streaming sink asked to short-circuit the run.",
        "type": "boolean"
      },
      "supervisor_control": {
        "anyOf": [
          {
            "$ref": "#/$defs/ControlAddress"
          },
          {
            "type": "null"
          }
        ],
        "default": null,
        "description": "Where an `oneharness interrupt` process addresses the controllable turn\nthis run's **supervisor** side opened, or `null` when control was not asked\nfor.\n\nA second field rather than a second meaning for [`Report::control`]: the two\nparties run under different configs on different session names, so they are\ndifferent sockets, refusable independently. A reader that has one address\nand assumes the other holds a lever it does not have.\n\nSerialized even when null, and for the reason [`Report::control`] gives."
      },
      "supervisor_control_unavailable": {
        "description": "Why an *asked-for* supervisor-side control channel is missing, when one is.\nThe companion of [`Report::supervisor_control`], exactly as\n`control_unavailable` is of `control`.",
        "type": [
          "string",
          "null"
        ]
      },
      "telemetry": {
        "anyOf": [
          {
            "$ref": "#/$defs/Telemetry"
          },
          {
            "type": "null"
          }
        ],
        "description": "Timing, per-party usage, and native oneharness session linkage."
      },
      "transcript": {
        "$ref": "#/$defs/Transcript",
        "description": "The full conversation transcript, with tool events on assistant turns."
      },
      "usage": {
        "anyOf": [
          {
            "$ref": "#/$defs/Usage"
          },
          {
            "type": "null"
          }
        ],
        "description": "Aggregated usage across every provider call (`None` if nothing reported)."
      },
      "verdicts": {
        "description": "The verdicts scored against the transcript, in the order they were added.",
        "items": {
          "$ref": "#/$defs/NamedVerdict"
        },
        "type": "array"
      }
    },
    "required": [
      "schema_version",
      "transcript",
      "stopped_early"
    ],
    "title": "Report",
    "type": "object"
  },
  "run_config": {
    "$defs": {
      "EvalConfig": {
        "additionalProperties": false,
        "description": "One eval scored against the finished transcript.",
        "properties": {
          "criterion": {
            "description": "The plain-English criterion.",
            "type": "string"
          },
          "kind": {
            "$ref": "#/$defs/JudgeKind",
            "default": "boolean",
            "description": "Boolean (a pass/fail verdict) or numeric (a score on a scale)."
          },
          "scale": {
            "default": null,
            "description": "The inclusive `[min, max]` scale for a numeric eval; ignored (and rejected)\nfor a boolean one. Defaults to `[0, 10]`.",
            "items": {
              "format": "double",
              "type": "number"
            },
            "maxItems": 2,
            "minItems": 2,
            "type": [
              "array",
              "null"
            ]
          }
        },
        "required": [
          "criterion"
        ],
        "type": "object"
      },
      "JudgeKind": {
        "description": "The kind of judgement requested.",
        "oneOf": [
          {
            "const": "boolean",
            "description": "A yes/no verdict.",
            "type": "string"
          },
          {
            "const": "numeric",
            "description": "A score on a `[min, max]` scale.",
            "type": "string"
          }
        ]
      },
      "ProviderConfig": {
        "additionalProperties": false,
        "description": "Which backend runs the harness. A flat, strict struct (rather than an\ninternally-tagged enum, which serde cannot pair with `deny_unknown_fields`):\n[`ProviderConfig::resolve`] checks that only the chosen `kind`'s fields are set\nand turns it into a validated [`ProviderSpec`], so a misplaced-but-spelled-right\nkey (e.g. `bin` under `kind: command`) is still a loud error.",
        "properties": {
          "args": {
            "default": null,
            "description": "`llmlint`: extra arguments appended to every `llmlint lint` invocation\nafter the ones onejudge composes (`--rule NAME`, `--agent NAME`, \u2026).",
            "items": {
              "type": "string"
            },
            "type": [
              "array",
              "null"
            ]
          },
          "bin": {
            "default": null,
            "description": "`oneharness`: the `oneharness` binary (default `oneharness`). `llmlint`:\nthe `llmlint` executable (default `llmlint`), probed with `--version` when\nthe run is built, so an absent one is a config error before any turn.",
            "type": [
              "string",
              "null"
            ]
          },
          "command": {
            "default": null,
            "description": "`command`: the provider argv (program + args).",
            "items": {
              "type": "string"
            },
            "type": [
              "array",
              "null"
            ]
          },
          "config": {
            "default": null,
            "description": "`llmlint`: the llmlint config file, passed as `llmlint lint -c <path>` as\ngiven. Unset, llmlint discovers its own (`llmlint.yml` up from the\nworktree it lints) \u2014 the repository's default.",
            "type": [
              "string",
              "null"
            ]
          },
          "control": {
            "default": null,
            "description": "`oneharness`: ask for **controllable** turns \u2014 both parties' calls add\n`oneharness run --control`, opening an out-of-band socket each that a\nseparate `oneharness interrupt` process can redirect the in-flight turn\nthrough. Default `false`, and `false` changes nothing about the run. The\naddresses land on the report's `control` (agent) and `supervisor_control`\n(judge) blocks \u2014 separate sockets, separately refusable; onejudge never\ninterrupts anything itself. See `docs/control.md`.",
            "type": [
              "boolean",
              "null"
            ]
          },
          "diff_base": {
            "default": null,
            "description": "`llmlint`: review only what the worktree changed against this git\nrevision (`llmlint lint --diff --diff-base <ref>`). onejudge does no base\nauto-detection: a host wiring a stack passes its own comparison base here.\nUnset, llmlint judges the whole tree.",
            "type": [
              "string",
              "null"
            ]
          },
          "judge": {
            "anyOf": [
              {
                "$ref": "#/$defs/ProviderConfig"
              },
              {
                "type": "null"
              }
            ],
            "description": "`split`: the one judge that judges and plays the simulated user \u2014 the\none-element shorthand for `judges: [<this>]`. Exclusive with `judges`."
          },
          "judge_config": {
            "default": null,
            "description": "`oneharness`: the oneharness config file the judge / simulated user run\nunder, passed as `oneharness run --config <path>` (default\n`oneharness.judge.toml`). This is where the judge-side harness/model\nselection lives.",
            "type": [
              "string",
              "null"
            ]
          },
          "judges": {
            "description": "`split`: the judges, one or more, every one run concurrently against each\nworker turn and combined into one attributed answer (see `docs/judges.md`).\nExclusive with `judge`; must not be empty.",
            "items": {
              "$ref": "#/$defs/ProviderConfig"
            },
            "type": [
              "array",
              "null"
            ]
          },
          "kind": {
            "$ref": "#/$defs/ProviderKind",
            "description": "`oneharness` (default) | `command` | `split` | `llmlint` (a judge entry\nonly)."
          },
          "label": {
            "default": null,
            "description": "A judge entry only: this judge's name on every surface \u2014 the `## Judge`\nheader the worker reads, the `[<label>]` reason prefix, the report's\n`judge_decisions`, and the `-<label>` session suffix. `[A-Za-z0-9_-]+`,\nunique within the list; defaults to the entry's `kind` for the first judge\nof that kind and `<kind>-<n>` for repeats.",
            "type": [
              "string",
              "null"
            ]
          },
          "mock_harness": {
            "default": null,
            "description": "`oneharness`: harness ids to run against **oneharness's own deterministic\n`MOCK_*` responder** instead of a paid model (`oneharness run --mock-harness\n<id>`, repeatable). Empty \u2014 the default \u2014 bills the real model.\n\nThis is how an acceptance proof that needs a real multi-turn chain runs for\nfree: only the model is scripted, everything else is the real oneharness. It\napplies to the agent side *and* the judge side, so each id must be one the\nconfig for that side selects (use `kind: split` when the two sides need\ndifferent ids), and it makes the run **spawn** `oneharness` \u2014 the responder is\noneharness's own binary re-executed, which an in-process run cannot be. See\n`docs/cli.md`.",
            "items": {
              "type": "string"
            },
            "type": [
              "array",
              "null"
            ]
          },
          "skill": {
            "anyOf": [
              {
                "$ref": "#/$defs/ProviderConfig"
              },
              {
                "type": "null"
              }
            ],
            "description": "`split`: the backend that runs the agent's turns."
          },
          "stream": {
            "default": null,
            "description": "`oneharness`: the agent-side binary speaks the **streamed provider\nprotocol** (`docs/streaming.md`) \u2014 NDJSON tool events as they occur, then a\nterminal report line. Default `false` (one buffered report document).",
            "type": [
              "boolean",
              "null"
            ]
          }
        },
        "type": "object"
      },
      "ProviderKind": {
        "description": "The provider backends the CLI can build. One enum is the single source for\nboth the YAML `kind:` (via `Deserialize`) and the `--provider` flag (via\nclap's `ValueEnum`), so the two surfaces cannot drift.",
        "oneOf": [
          {
            "const": "oneharness",
            "description": "Shell out to the `oneharness` CLI (the default).",
            "type": "string"
          },
          {
            "const": "command",
            "description": "A custom command speaking the JSON-lines protocol.",
            "type": "string"
          },
          {
            "const": "split",
            "description": "Compose a skill-runner with a separate judge / simulated-user backend.",
            "type": "string"
          },
          {
            "const": "llmlint",
            "description": "A judge whose whole verdict is one `llmlint` run over the worker's tree\n(`docs/judges.md`). Judge-side only: valid under `judges:` / `judge:` of a\n`split`, refused as the top-level provider, under `skill:`, and as the\n`--provider` / `ONEJUDGE_PROVIDER` override.",
            "type": "string"
          }
        ]
      },
      "UserConfig": {
        "additionalProperties": false,
        "description": "The simulated user that supervises the agent and drives the loop.",
        "properties": {
          "artifacts": {
            "default": [],
            "description": "Files or directories every judge-side prompt names for the evaluator to\nread directly \u2014 work that may be untracked or gitignored, which\n`git_status` and `git_diff` cannot show. An absolute path is used as\nwritten; a relative one is resolved against the skill's working directory.\nA directory is listed newest-modified first, re-read every judge-side\nturn; a path that does not exist is named as such. Replaced by\n`--artifact` / `ONEJUDGE_ARTIFACTS`.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "done_when": {
            "default": null,
            "description": "A plain-English completion condition; when the judge decides it holds, the\nloop ends. Without it the loop ends at `max_turns` or when the agent\ndeclares itself done.",
            "type": [
              "string",
              "null"
            ]
          },
          "max_turns": {
            "default": null,
            "description": "The assistant-turn cap for this run.",
            "format": "uint32",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "persona": {
            "default": "",
            "description": "How the simulated user behaves (their instructions).",
            "type": "string"
          },
          "settle_on_noop": {
            "default": null,
            "description": "Whether a run of quiet, repeated exchanges settles the loop early.\nDefaults to `true` \u2014 omitting the key is exactly what every config has\nalways done. Set it `false` when reporting nothing *is* this\nconversation's contract (a long-lived observer answering with one fixed\nshort sentence while it finds nothing), so the loop is driven on to\n`max_turns` instead of being settled on its second quiet exchange.",
            "type": [
              "boolean",
              "null"
            ]
          }
        },
        "type": "object"
      }
    },
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "description": "The whole YAML config for one run. Field defaults let a minimal file (just a\n`task`) work, while `deny_unknown_fields` makes a typo'd key a hard error\ninstead of a silently-ignored setting.",
    "properties": {
      "assessment": {
        "default": null,
        "description": "Optional prompt for a free-text judgement of the finished transcript.",
        "type": [
          "string",
          "null"
        ]
      },
      "evals": {
        "description": "Optional criteria to score the finished transcript with.",
        "items": {
          "$ref": "#/$defs/EvalConfig"
        },
        "type": "array"
      },
      "provider": {
        "$ref": "#/$defs/ProviderConfig",
        "description": "Which backend runs the harness (and judges / plays the user).\n\nHarness/model **selection** is no longer a onejudge concern: the agent side\nuses oneharness's discovered `oneharness.toml`, and the judge side uses the\n`provider.judge_config` file (default `oneharness.judge.toml`). Scaffold both\nwith `onejudge init`."
      },
      "session": {
        "default": null,
        "description": "The caller-owned session name threaded across turns; defaults to\n`onejudge`.",
        "type": [
          "string",
          "null"
        ]
      },
      "skill": {
        "default": null,
        "description": "Path to a **skill** directory (containing a `SKILL.md`) whose instruction\nbody seeds the system prompt. Resolved relative to the config file's\ndirectory (or the working dir for a flag-only run). Optional \u2014 combine it\nwith `system_prompt`, use either alone, or neither.",
        "type": [
          "string",
          "null"
        ]
      },
      "system_prompt": {
        "default": null,
        "description": "Extra system-prompt text for the harness. When a `skill` is also set, this\ncomes **first** and the skill's body is appended after it.",
        "type": [
          "string",
          "null"
        ]
      },
      "task": {
        "default": null,
        "description": "The task to drive to completion. May instead be supplied by `--task`\n(`-` reads stdin); required by the time the plan is built.",
        "type": [
          "string",
          "null"
        ]
      },
      "user": {
        "anyOf": [
          {
            "$ref": "#/$defs/UserConfig"
          },
          {
            "type": "null"
          }
        ],
        "description": "The simulated user / supervisor that drives the loop. Omit for a\nsingle-turn run (the agent answers once)."
      }
    },
    "title": "Config",
    "type": "object"
  },
  "stream_event": {
    "$defs": {
      "ToolEvent": {
        "description": "One normalized tool-call / action event the skill took during a turn, lifted\nfrom `oneharness`'s `events` array (its `--events` output). Harness-agnostic,\nso a consumer can inspect shell commands, file edits, and tool uses across any\nharness, not just the final text. `input` is the structured, tool-shaped args\nso a consumer can match on the command string or file path without re-parsing.\n\n`input` is free-form JSON, so `Message`/`Transcript` are `PartialEq` but not\n`Eq`.",
        "properties": {
          "index": {
            "default": 0,
            "description": "Position within the run, so ordering (\"did X before Y\") is expressible.",
            "format": "uint",
            "minimum": 0,
            "type": "integer"
          },
          "input": {
            "description": "Structured tool arguments (the command, the file path); `None` when none."
          },
          "kind": {
            "description": "`tool_call` (the skill invoked a tool) or `tool_result` (the observation).",
            "type": "string"
          },
          "name": {
            "description": "Normalized tool name where knowable (e.g. `bash`, `edit_file`); `None` for\na `tool_result` or when the harness did not name it.",
            "type": [
              "string",
              "null"
            ]
          },
          "output": {
            "description": "The result/observation text, when the transcript exposed it.",
            "type": [
              "string",
              "null"
            ]
          },
          "tool_call_id": {
            "description": "The harness's own stable identity for this call, joining a `tool_call` to\nits `tool_result`. `None` means the harness exposed no identity \u2014 never an\nempty string, which would read as an id that happens to be blank.",
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "kind",
          "index"
        ],
        "type": "object"
      }
    },
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "description": "One streamed tool event delivered live to an [`Engine::run_streaming`] sink,\ntagged with the turn it belongs to.\n\nIt is also the payload of an `event` line in the **streamed protocol**\n(`docs/streaming.md`) that `onejudge run --stream` publishes \u2014 the line adds\nthe `\"type\": \"event\"` discriminator around exactly these fields, so an SDK\nhands this type straight to its own event callback.",
    "properties": {
      "event": {
        "$ref": "#/$defs/ToolEvent",
        "description": "The normalized tool event."
      },
      "turn": {
        "description": "1-based assistant-turn index within this run.",
        "format": "uint",
        "minimum": 0,
        "type": "integer"
      }
    },
    "required": [
      "turn",
      "event"
    ],
    "title": "StreamEvent",
    "type": "object"
  }
}
